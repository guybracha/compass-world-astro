---
import BaseLayout from '../../layouts/BaseLayout.astro';

// ✅ תמיכה בתת-נתיב (כמו /compass-world-astro/)
const BASE = import.meta.env.BASE_URL || '/';
const withBase = (p = '') => {
  if (!p) return '';
  if (/^https?:\/\//i.test(p)) return p;   // קישורים חיצוניים
  if (p.startsWith(BASE)) return p;        // כבר כולל BASE
  const b = BASE.replace(/\/+$/, '');
  const path = String(p).replace(/^\/+/, '');
  return `${b}/${path}`;
};
---

<BaseLayout title="אודות | עולם המצפן" description="קצת על היקום ועל היוצר">
  <section class="container py-5" data-reveal>
    <h1 class="display-6 mb-3">אודות</h1>

    <p class="text-white-80">
      <strong>עולם המצפן</strong> הוא יקום חי של מד&quot;ב ופנטזיה —
      דמויות, תקופות ומסתורי הקוסמוס — שנבנה ומתפתח לאורך זמן.
      בעמוד זה תוכלו לקרוא בקצרה על הפרויקט ועל הכוונה מאחוריו.
    </p>

    <div class="card pc-glow p-4 mt-4">
      <h2 class="h5 mb-2">על היוצר</h2>
      <p class="text-white-50 mb-0">
        גיא ברכה — יוצר, מאייר, וכותב. אם תרצו ליצור קשר, מוזמנים לשלוח
        <a class="link-light text-decoration-underline" href="mailto:guy.bracha@gmail.com">אימייל</a>
        או להציץ בגלריה.
      </p>
      <div class="mt-3">
        <a class="btn btn-sm btn-outline-light me-2" href={withBase('/gallery/')}>לגלריה</a>
        <a class="btn btn-sm btn-brand" href={withBase('/comics/')}>לקומיקס</a>
      </div>
    </div>
  </section>

  <style is:global>
    .pc-glow { background: rgba(255,255,255,.05); border: 1px solid rgba(255,255,255,.12); }
  </style>
</BaseLayout>
